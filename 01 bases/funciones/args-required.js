"use strict";
(() => {
    //Argumentos obligatorios firstName:string, lastName:string
    const fullName = (firstName, lastName) => {
        return `${firstName} ${lastName}`;
    };
    const name = fullName('Tony', 'Stark');
})();
//# sourceMappingURL=args-required.js.map